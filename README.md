# Readme
I really don't like writing these.....
## Setup And Use
To run the app like both the frontend and the backend just use `npm run dev` in the root folder of the repo.
You also have to edit create the .env and like this:
`
DATABASE_URL="random aa postgres database because i like postgres(its free)"
PORT=3001
`
So then you can do the run dev again and it will function.

## Could you pretty please implement these?
 1. Currently the text is rendered as a overlay so its really clunk when you drag it around. I fixed the drawing doing that but the text i can't seem to do it.
 2. Please make the snapping to curve better

 
